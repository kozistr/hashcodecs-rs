version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"
    rust: "1.91"

mkdocs:
  configuration: mkdocs.yaml
  fail_on_warning: true

python:
  install:
    - requirements: docs/requirements.txt
    - method: pip
      path: .
